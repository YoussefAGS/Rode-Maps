  Skip to content
    Search or jump to…
    Pull requests
    Issues
    Codespaces
    Marketplace
    Explore

    @YoussefAGS
    RobertBrunhage
    /
    flutter-github-action
    Public
    Fork your own copy of RobertBrunhage/flutter-github-action
    Code
    Issues
    Pull requests
    2
    Actions
    Projects
    Security
    Insights
    flutter-github-action/.github/workflows/ci.yml
    @RobertBrunhage
    RobertBrunhage Add no codesign for iOS
    Latest commit 914ce62 on Sep 13, 2020
    History
    1 contributor
    53 lines (50 sloc)  1.24 KB

  name: CI
  on:
    pull_request:
      branches:
        - master

  jobs:
    flutter_test:
      name: Run flutter test and analyze
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v2
        - uses: actions/setup-java@v1
          with:
            java-version: "12.x"
        - uses: subosito/flutter-action@v1
          with:
            channel: "stable"
        - run: flutter pub get
        - run: flutter analyze
        - run: flutter test

    build_ios:
      name: Build Flutter (iOS)
      needs: [flutter_test]
      runs-on: macos-latest
      steps:
        - uses: actions/checkout@v2
        - uses: actions/setup-java@v1
          with:
            java-version: "12.x"
        - uses: subosito/flutter-action@v1
          with:
            channel: "stable"
        - run: flutter pub get
        - run: flutter clean
        - run: flutter build ios --release --no-codesign

    build_appbundle:
      name: Build Flutter (Android)
      needs: [flutter_test]
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v2
        - uses: actions/setup-java@v1
          with:
            java-version: "12.x"
        - uses: subosito/flutter-action@v1
          with:
            channel: "stable"
        - run: flutter pub get
        - run: flutter clean
        - run: flutter build appbundle
